# /etc/nginx/conf.d/connection-upgrade.conf
#
# Required by the WebSocket / EventSource upgrade headers used in
# mail.saltnlightllc.com.conf. Lives at conf.d/ level so it is included
# in the http{} block before the server{} blocks load.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
